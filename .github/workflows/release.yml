name: Release

on:
  push:
    branches: [main]

jobs:
  release:
    uses: EliasSchlie/claude-plugins/.github/workflows/plugin-release.yml@main
    with:
      plugin-name: sub-claude
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
